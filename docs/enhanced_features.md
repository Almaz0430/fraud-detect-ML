# Расширенные AI-функции системы детекции мошенничества

## 🎯 Обзор новых возможностей

Система была расширена следующими AI-функциями:

1. **Расширенные объяснения** - Детальный анализ через Gemini с примерами похожих случаев
2. **Многоязычная поддержка** - Объяснения на русском, английском и казахском языках
3. **Conversational AI** - Чат-бот для вопросов о транзакциях
4. **Анализ аномалий** - Детальное объяснение аномальных паттернов
5. **Recommendation engine** - Предложения по снижению рисков

## 🔧 Новые API эндпоинты

### 1. Расширенное объяснение
**POST** `/explain/enhanced`

Генерирует детальное объяснение с похожими случаями и рекомендациями.

```json
{
  "transaction": {
    "V1": -1.36, "V2": -0.07, ..., "V28": -0.02,
    "Amount": 149.62
  },
  "threshold": 0.5,
  "language": "ru"
}
```

**Ответ:**
```json
{
  "explanation": "Детальное объяснение на выбранном языке...",
  "similar_cases": [...],
  "anomalies": {...},
  "recommendations": [...],
  "fraud_score": 0.85,
  "is_fraud": true,
  "language": "ru"
}
```

### 2. Поиск похожих случаев
**POST** `/similar-cases`

Находит исторические случаи, похожие на текущую транзакцию.

```json
{
  "transaction": {...},
  "top_k": 5
}
```

### 3. Анализ аномалий
**POST** `/analyze-anomalies`

Детальный анализ аномальных паттернов в транзакции.

```json
{
  "transaction": {...},
  "threshold": 0.5
}
```

**Ответ:**
```json
{
  "anomalies": {
    "detected_anomalies": [
      {
        "type": "high_amount",
        "description": "Необычно высокая сумма: 5000",
        "severity": "high"
      }
    ],
    "severity_level": "high",
    "anomaly_score": 0.9,
    "pattern_types": ["large_transaction"]
  }
}
```

### 4. Рекомендации по рискам
**POST** `/recommendations`

Генерирует рекомендации по снижению рисков.

```json
{
  "transaction": {...},
  "threshold": 0.5,
  "language": "ru"
}
```

**Ответ:**
```json
{
  "recommendations": [
    {
      "type": "immediate",
      "action": "Немедленно заблокировать транзакцию"
    },
    {
      "type": "investigation", 
      "action": "Провести детальное расследование"
    }
  ]
}
```

### 5. Обратная связь
**POST** `/feedback`

Отправка обратной связи для улучшения модели.

```json
{
  "transaction": {...},
  "prediction_result": {...},
  "feedback": true
}
```

### 6. Чат-бот
**POST** `/chat`

Интерактивный чат-бот для консультаций.

```json
{
  "message": "Как работает система детекции?",
  "session_id": "user_123",
  "language": "ru",
  "transaction_context": {
    "transaction": {...},
    "result": {...}
  }
}
```

**Ответ:**
```json
{
  "response": "Система использует машинное обучение...",
  "suggestions": [
    "Что означают признаки V1-V28?",
    "Как защитить себя от мошенников?"
  ],
  "session_id": "user_123"
}
```

### 7. Статистика чат-бота
**GET** `/chat/stats`

Получение статистики использования чат-бота.

### 8. Очистка сессии
**DELETE** `/chat/clear/<session_id>`

Очистка сессии чат-бота.

## 🌐 Многоязычная поддержка

Система поддерживает три языка:
- `ru` - Русский (по умолчанию)
- `en` - English
- `kk` - Қазақша

Все объяснения, рекомендации и чат-бот работают на выбранном языке.

## 🤖 Чат-бот возможности

Чат-бот может:
- Отвечать на общие вопросы о системе
- Анализировать конкретные транзакции
- Давать советы по безопасности
- Объяснять работу алгоритмов
- Поддерживать контекст разговора

### Режимы работы:
- **GENERAL** - Общие вопросы
- **TRANSACTION_ANALYSIS** - Анализ транзакций
- **SECURITY_ADVICE** - Советы по безопасности
- **SYSTEM_HELP** - Помощь по системе

## 🔍 Анализ аномалий

Система автоматически обнаруживает:
- **Высокие суммы** - Транзакции выше обычного
- **Микротранзакции** - Очень маленькие суммы
- **Экстремальные признаки** - Значения за пределами нормы
- **Временные аномалии** - Необычное время транзакций

### Уровни серьезности:
- **LOW** - Низкий риск
- **MEDIUM** - Средний риск  
- **HIGH** - Высокий риск

## 💡 Система рекомендаций

Генерирует рекомендации по типам:
- **IMMEDIATE** - Немедленные действия
- **INVESTIGATION** - Расследование
- **CONTACT** - Связь с клиентом
- **MONITORING** - Усиленный мониторинг
- **VERIFICATION** - Дополнительная проверка
- **LIMITS** - Изменение лимитов

## 🔄 Поиск похожих случаев

Использует косинусное сходство для поиска похожих транзакций в исторической базе:
- Сравнивает признаки V1-V28 и Amount
- Возвращает топ-K наиболее похожих случаев
- Показывает описание и тип паттерна
- Указывает результат (мошенничество/легитимная)

## 📊 Обратная связь

Система собирает обратную связь для:
- Улучшения точности модели
- Создания базы размеченных случаев
- Автоматического переобучения
- Анализа качества предсказаний

## ⚙️ Настройка

### Переменные окружения:
```bash
# Gemini API
GEMINI_API_KEY=your_api_key
GEMINI_MODEL=gemini-2.0-flash-exp
GEMINI_TEMPERATURE=0.3
GEMINI_MAX_TOKENS=1000

# Чат-бот
GEMINI_CHATBOT_MODEL=gemini-2.0-flash-exp
```

### Файлы конфигурации:
- `data/historical_cases.json` - База исторических случаев
- Автоматически создается при первом запуске

## 🚀 Быстрый старт

1. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

2. **Настройте API ключ:**
```bash
export GEMINI_API_KEY="your_api_key"
```

3. **Запустите API:**
```bash
python -m app.api
```

4. **Тестируйте новые функции:**
```bash
python examples/enhanced_api_examples.py
```

## 📝 Примеры использования

### Python клиент:
```python
import requests

# Расширенное объяснение
response = requests.post("http://localhost:5000/explain/enhanced", json={
    "transaction": {...},
    "language": "ru"
})

# Чат с ботом
response = requests.post("http://localhost:5000/chat", json={
    "message": "Объясни эту транзакцию",
    "session_id": "user_123",
    "language": "ru"
})
```

### cURL:
```bash
# Анализ аномалий
curl -X POST http://localhost:5000/analyze-anomalies \
  -H "Content-Type: application/json" \
  -d '{"transaction": {...}}'

# Рекомендации
curl -X POST http://localhost:5000/recommendations \
  -H "Content-Type: application/json" \
  -d '{"transaction": {...}, "language": "en"}'
```

## 🔧 Интеграция с существующим кодом

Новые функции полностью совместимы с существующим API. Старые эндпоинты продолжают работать без изменений.

## 📈 Производительность

- **Расширенные объяснения**: ~2-5 секунд (зависит от Gemini API)
- **Поиск похожих случаев**: ~50-100ms
- **Анализ аномалий**: ~10-20ms
- **Чат-бот**: ~1-3 секунды
- **Рекомендации**: ~20-50ms

## 🛠️ Разработка

Для добавления новых функций:
1. Расширьте `enhanced_llm.py` или `chatbot.py`
2. Добавьте новые эндпоинты в `api.py`
3. Обновите документацию
4. Добавьте тесты в `examples/`

## 🐛 Устранение неполадок

### Частые проблемы:

1. **Gemini API недоступен**
   - Проверьте GEMINI_API_KEY
   - Убедитесь в наличии интернета

2. **Чат-бот не отвечает**
   - Проверьте логи API
   - Убедитесь в корректности session_id

3. **Похожие случаи не найдены**
   - База создается автоматически
   - Добавьте больше исторических данных

4. **Ошибки многоязычности**
   - Поддерживаются только: ru, en, kk
   - Проверьте параметр language
